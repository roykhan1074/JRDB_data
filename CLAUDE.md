# RACE INSIGHT — Claude Code 作業ルール

このファイルは **すべての会話で必ず読み込まれる**。
以下のルールを破ることは禁止。同じミスを繰り返さないこと。

---

## 🚨 UI実装の鉄則（何度も違反している・最優先で守ること）

**新しいUI要素（セル・バッジ・色・関数）を追加するとき、色を自分で決めてはならない。**
必ず先に `entries.html` の既存の同種コードを読み、そのままコピーして使う。

### 具体的な手順（省略禁止）

1. `entries.html` で既存の類似関数（`anabaBg`, `pacefitBg`, `rankCellBg` 等）を Grep で探す
2. その関数の色・閾値をそのままコピーする
3. 色を自分で考えない・変えない・「合いそう」で選ばない

### なぜ繰り返すのか（自己分析）

「ルールを知っている」だけでは不十分。実装時に既存コードを確認するステップを省略しているため。
新しい指数・セル・バッジを作るたびに必ず既存コードを読む。これは省略できない。

---

## ⛔ UIカラー規約（最重要・何度も間違えている）

出馬表（entries.html）で A〜E グレードを表示するとき、色は必ず以下の規約に従う。
**勝手に変えない。** 既存の `sp-a`〜`sp-e` クラスがこの規約の正典。

| グレード | 背景色 | 文字色 | クラス参照 |
|---|---|---|---|
| S | `#ca8a04` | `#fff` | （特別枠） |
| A | `#dc2626` | `#fff` | `sp-a` 赤 |
| B | `#15803d` | `#fff` | `sp-b` 緑 |
| C | `#6b7280` | `#fff` | `sp-c` 系 |
| D | `#374151` | `#fff` | `sp-d` 系 |
| E | `#e5e7eb` | `#6b7280` | `sp-e` 薄グレー |

セル背景を使う場合:
- A → `#fbcfe8`（ピンク）
- B → `#bbf7d0`（薄緑）
- E → `#e5e7eb`（薄グレー）
- C/D → なし（白）

### 指数セル背景色（スコア系）の統一規約

`anabaBg`・`honmeiBg`・`pacefitBg` など、数値指数のセル背景色は **ピンク/黄色/グレー** に統一する。**青系（`#bfdbfe`, `#dbeafe` 等）は絶対に使わない。**

| 閾値 | 色 | カラーコード |
|---|---|---|
| 上位（高スコア） | ピンク | `#fbcfe8` |
| 中位 | 黄色 | `#fef9c3` |
| 0以上低スコア | 白（なし） | — |
| 負スコア | 薄グレー | `#e5e7eb` |
| 最上位（超高スコア） | 薄赤 | `#f87171`（EX指数100+のみ） |

**よくやるミスの例:**
- A=緑、E=赤 にする（逆！）
- B を青に変える（禁止）
- アンバー・オレンジを C/D に使う（禁止）
- 新しいグレード体系を作るとき、A〜E の色を既存規約から変える（禁止）
- **新しい指数を追加するとき、青系（`#bfdbfe`など）を使う（禁止）→ ピンク/黄色を使う**

---

## テーブル設計規約

- `table-layout: fixed` を必ず使う
- テーブル本体の `width` は **col の合計幅と同じ px 値を明示する**（`width:100%` 禁止。`width:auto` も下記の事故を踏まえ禁止）。
  - **実際に発生した事故①**: entries.html で `width:100%` になっていたため、col幅の合計が `table-wrap` の表示幅を超えても `table-layout:fixed` が全列を比率縮小してしまい、右端の列（複勝など）が見切れて `overflow-x:auto` のスクロールも効かなくなった。
  - **実際に発生した事故②（watchlist.html のようにレース毎に独立した`<table>`を複数生成するページ）**: `table-layout:fixed` + `width:auto` の組み合わせでは、`white-space:nowrap` により改行不可能なセル内容（外国人騎手名「Ａ．シュタル」など）が colgroup の指定幅より広い場合、そのテーブルだけ列が拡大し、他のテーブル（他レース）と列位置がズレた。原因はCSS仕様上 `width:auto` の fixed テーブルは内容の最小幅（GRIDMIN）を考慮してしまうため。`overflow:hidden` をセルに付けても防げない。**修正**: `table { width: 930px }` のように colgroup 合計幅を数値でそのまま指定する（`auto` にしない）。
  - 列を追加・幅変更するたびに `table { width: ... }` が colgroup 合計と一致しているか必ず確認する
  - **同一ページ内に同じ構造の `<table>` を複数（レース毎・グループ毎など）生成する場合、列幅を1つのテーブルだけで確認して終わらせない。** 全テーブルで列幅が揃っているかを実機検証で確認する（下記「UI変更時の必須チェック」参照）。特に外国人騎手名・長い固有名詞など nowrap で改行できない長い文字列を含む行がどこかに1つでもあると、そのテーブルだけ列がズレる典型パターンなので、サンプルデータは複数レース分（できれば長い文字列を含むレースを含む）で検証する
- すべての列を `<colgroup><col style="width:Xpx">` で明示する（幅未指定列の禁止）
- スクロールは `table-wrap` の `overflow-x: auto` で対応する
- `white-space: nowrap` と `overflow: hidden` をセルに適用する
- ヘッダーは `rowspan` / `colspan` で構造化する

### ヘッダー文字列の折り返しルール（絶対厳守・何度も指摘されている）

ヘッダーの折り返しは見た目が悪いため、極力減らす。列を新規追加・変更するときは必ずこのルールで列幅を設計する。

- **ヘッダー文字列が4文字以内 → 折り返し禁止**（`class="hdr-nowrap"` を付与し、1行に収まる列幅を確保する）
- **ヘッダー文字列が5文字以上 → 折り返し可。ただし最大2行まで**。3行以上になる場合は列幅が不足しているので広げる。2行に分ける位置は `<br>` で明示的に指定し、`word-break: break-all` の自動改行に任せない（意図しない1文字ずつの縦書き状態になる）
- 目安: 全角3文字を1行に収めるには列幅 56px 程度が必要（padding・letter-spacing込み）。48px では全角3文字がギリギリ折り返される
- 新しい列を既存の短い列（厩指数・調教SP等）の隣に追加する場合、隣接列も巻き込んで折り返していないか確認する。列幅の変更は「既存レイアウトを壊す」のではなく「折り返しルールを満たす」ための調整であり、この目的の変更は許される

---

## ⛔ UI変更時の必須チェック（何度もレイアウト崩れを起こし、ユーザーの時間を大幅に浪費した）

**「ブラウザレンダリングを目視確認できないので、ユーザーに確認してもらう」は禁止。**
実際にはこのマシンにChromeが入っており、ヘッダーの折り返し・列幅・横スクロールの可否は
**すべてこちらで実機検証できる**。目視の代わりに以下の手順を使わずに完了報告することは許されない。

**過去の失敗**: `width:100%`→`width:auto`への修正だけで「直った」と3回報告したが、
実際には`table-layout:fixed`が`width:auto`でも列幅を比例縮小する挙動を見落としており、
狭い画面で全ヘッダーが折り返す状態のまま何度もユーザーに再指摘させてしまった。
最終的に`width:max-content`が正しい修正で、これはPuppeteer実機検証で初めて確定できた。
**目視や仕様の記憶だけで「直った」と判断せず、必ず実機で数値検証すること。**

### 1. 列数の整合チェック（構造ミスの検出）
`col数 = 実効th数（colspan展開後）= td数` が一致しているか数える。
一致しない場合は原因を突き止めてから報告する。

### 2. レンダリング実機検証（見た目の検証・省略禁止）
テーブルの列幅・ヘッダー折り返し・横スクロールに関わる変更をしたら、必ず以下を実行してから完了報告する。

```bash
# 初回のみ（システムのChromeを使うので毎回インストール不要、使い終わったら削除）
npm install --no-save puppeteer-core
```

Node script（例）で以下を検証する:
- `document.createRange().selectNodeContents(th); range.getClientRects()` の `top` 座標の
  ユニーク数 = 実際の行数（`getBoundingClientRect().height`は`rowspan`等で偽装されるため使えない）
- ヘッダーが4文字以内なら行数が1であること、5文字以上なら1〜2であることを**全ヘッダーについて**確認する
- 複数のビューポート幅（例: 1600px, 700px, 500px）で検証し、狭い画面でも列幅が縮小されていないか確認する
  （`th.getBoundingClientRect().width` が意図した値のままか）
- `table-wrap.scrollWidth > table-wrap.clientWidth` で横スクロールが実際に発生するか確認する
- 必要なら `page.screenshot()` で目視確認も行う

検証後は一時ファイル（`.scratch/`等）と`puppeteer-core`を削除する。
**この実機検証をせずに「レイアウトをご確認ください」とだけ書いて報告を終えることは禁止。**
実機検証で確認済みの場合は「Puppeteerで実機検証済み」と明記し、確認できなかった項目のみユーザーに確認を求める。

### 2. 出馬表テーブル 列マップ（正典）

列を追加・削除・非表示にするたびにこの表を必ず更新する。

| # | col幅 | ヘッダー | 備考 |
|---|-------|---------|------|
| 1 | 28px | 枠 | |
| 2 | 28px | 馬番 | |
| 3 | 46px | 予想（自分印） | 自分の予想印（プルダウン選択・T_MY_MARKに永続化、分析用） |
| 4 | 40px | 推奨（推奨印） | 推奨買い目の推奨順（1位◎2位○3位▲4位注5位以下△）。rankMarkHtml流用 |
| 5 | 160px | 馬名 | |
| 6 | 36px | B | hdr-nowrap。データセルは `初B`(初装着)/`再B`(再装着)を表示。T_BLINKER_SCORE.grade='A'の馬はセル背景ピンク(`#fbcfe8`) |
| 7 | **0px** | **EX指数全体** | **非表示中** |
| 8 | 56px | EX指数（コース） | group-sep col-divider, hdr-nowrap |
| 9 | **0px** | **本命指数全体** | **非表示中** |
| 10 | 48px | 本命指数（コース） | group-sep |
| 11 | 48px | 脚質 | group-sep, hdr-nowrap |
| 12 | 60px | オッズ | group-sep, hdr-nowrap |
| 13 | 76px | 騎手 | group-sep |
| 14 | 76px | 厩舎 | |
| 15 | **0px** | **騎×厩** | **非表示中** |
| 16 | 20px | 情報印（印） | group-sep col-divider, colspan=2開始, hdr-nowrap（th側） |
| 17 | 36px | 情報印（指数） | colspan=2の2列目 |
| 18 | 20px | 展開印（印） | group-sep, colspan=2開始, hdr-nowrap（th側） |
| 19 | 36px | 展開印（指数） | colspan=2の2列目 |
| 20 | 20px | IDM印（印） | group-sep, colspan=2開始 |
| 21 | 28px | IDM印（指数） | colspan=2の2列目 |
| 22 | 48px | 前3F | group-sep col-divider |
| 23 | 48px | 後3F | |
| 24 | 56px | 厩指数 | group-sep col-divider, hdr-nowrap（4文字以内は折り返さない） |
| 25 | 56px | 厩指穴信頼 | 厩舎指数≧0×基準オッズ≧15倍セグメントの厩舎別信頼度グレード（S~F）。n≧20のみ判定、ホバーで全体数/単勝回収率/複勝回収率/勝率/複勝率を表示。5文字のため`<br>`で「厩指穴」「信頼」の2行に手動改行（ヘッダー折り返しルール: 4文字以内は折り返さない、5文字以上は最大2行まで） |
| 26 | 56px | 調教SP | group-sep, hdr-nowrap |
| 27 | **0px** | **展開指数** | **非表示中** |
| 28 | 46px | 着順 | group-sep col-divider |
| 29 | 52px | 単勝 | |
| 30 | 52px | 複勝 | |

**合計: col=30、th実効列=30（colspan込み）、td=30**

### 3. 列の非表示方法（唯一の正しい方法）
```
<col style="width:0">                                          ← colで幅を0に
<th style="width:0;padding:0;border:0;overflow:hidden;font-size:0">  ← th
<td style="width:0;padding:0;border:0;overflow:hidden;font-size:0">  ← td
```
- `display:none` を `<th>` に使うと **colspan のカウントがずれる → 禁止**
- `visibility:collapse` はブラウザ互換性が低い → **禁止**

---

## ⛔ 内訳・ツールチップ表示の必須チェック（2026-09-06、EX指数/本命指数のコース内訳流用バグで発覚）

`entries.html`のEX指数・本命指数のツールチップが「全体」の内訳をそのまま「コース」欄にも表示していたため、表示値と内訳合計が食い違って見えるバグが発生した（コース別の内訳はDBに未保存なため）。さらに`T_ANABA_SCORE.score_kyusha`が`score_kyakushitsu`の重複値（未使用プレースホルダー）になっており、内訳合計が「全体」の値とすら一致しないケースもあった。

**教訓：** 内訳（ツールチップ等）を表示する機能を新規作成・改修するときは、**目視確認だけで完了報告しない**。以下を必ず実施する。

1. 内訳の各項目が実際に独立したファクター値を参照しているか（コピペで同じ列を2箇所に使っていないか）をSQL側の実装で確認する。
2. `SELECT COUNT(*) FROM T_X WHERE ROUND(内訳の合計,1) <> 表示値` を**DB全件**に対して実行し、0件であることを確認してから完了報告する（1〜2件のサンプル目視では不十分。実際にこの手順で複数の指数を横断監査し、EX指数/本命指数の2箇所だけに問題があり他は無傷であることを確認できた）。
3. 「全体」と「コース」のように**同じ指数名で複数のバリエーションがある**場合、それぞれ別々に内訳を保存・表示する。保存していない場合はツールチップにその旨を明記し、表示値と内訳合計が一致しない理由を隠さない（推測や黙認をしない）。

---

## 買い目推奨時の分析方針（複数ファクター必須）

レース毎の推奨買い目を作成するとき、**EX指数（anaba_overall_score / anaba_course_score）だけで判断しない**。
EX指数はあくまで複数ある評価軸の一つであり、以下のファクターも必ず確認し、総合的に評価すること。

| ファクター | フィールド | 観点 |
|---|---|---|
| EX指数 | `anaba_overall_score` / `anaba_course_score` | 各ファクターの単勝回収率偏差の合計（穴馬向け） |
| IDM | `idm` | 基礎能力の絶対値・レース内順位 |
| 情報指数 | `joho_index`（-1は非開示で除外） | 取材情報による評価 |
| 展開・脚質 | `goal_juni`, `ten_index_juni`, `agari_index_juni`, `kyakushitsu` | レースペースとの適性 |
| 調教 | `chokyo_yajirushi`, `oi_index`, `shiage_index` | 仕上がり・上昇度 |
| 1番人気信頼度 | `calcFavoriteReliability`相当の計算 | 1番人気がJRDB指数群に支持されているか（odds制限なし） |
| 騎手×厩舎複勝回収率 | `combo_win_rr` / `combo_place_rr` / `combo_n` | サンプル数を必ず確認（小サンプルは参考程度） |
| 厩舎指数穴セグメント | `kyusha_anaba_place_rr` / `kyusha_anaba_n` | サンプル数を必ず確認 |

- EX指数が高いことは「強さのシグナルの一つ」であり、odds帯に関わらず参考にする（odds<10倍では%への変換のみ控える）。
- 他ファクターと矛盾する場合（例: EX指数は高いが展開不向き・調教ダウン）は、その矛盾点を明示した上で結論を出す。
- 推奨理由には必ず「どのファクターが根拠か」を明記し、EX指数のみに依存した結論にしない。

---

## フィールド名規約

- 上がり指数順位 → `agari_index_juni`（`ato3f_juni` は使わない）
- 基準オッズ → `kijun_odds`
- 基準人気 → `kijun_ninki`

---

## ⛔ 指数/スコアETL実装時の必須チェック（2026-09-02、ブリンカー指数で実際に事故が発生）

EX指数・本命指数・展開適合指数・コース回収率指数・ブリンカー指数のような「馬ごとにスコアを計算してテーブルに保存するETL」（`sql/*_index.sql`）を新規実装・改修するときは、以下を必ず守る。信頼度の異なる3つの教訓がある。

### 1. 【確定・修正済み】スコア計算はT_SED等の結果テーブルに依存させない

出馬表（entries.html）は**まだ結果が出ていないレース**を見るためのページである。にもかかわらず`sql/blinker_index.sql`のPart3（スコア計算）は当初、Part2（過去データからの回収率集計）用に作った「T_SEDとINNER JOIN済みの一時テーブル」をそのまま使い回してしまい、**結果未確定のレースの馬にはスコアが一切付与されない**バグを埋め込んでいた（ユーザーが「グレードAのピンクセルの出現率が想定の30%を大きく下回っている」と実データで指摘して発覚。8月の初装着174件中41件がスコア欠落していた）。

- **ファクター集計（過去の回収率を求める段階）だけがT_SED/T_ANALYZE_FACTを必要とする。スコア計算（出走予定馬にその場で点数をつける段階）はT_KYI×T_BACだけで完結させ、結果テーブルを一切参照してはならない。**
- 新規ETLを書いたら、末尾に必ず**カバレッジ整合性チェック**を入れる：`対象母集団（例: blinker IN ('1','2')）の件数` と `スコアテーブルに実際に行がある件数` を比較し、差が0件であることを確認するSELECT。0件でなければAPIエンドポイント側で警告として画面に表示する（`src/server.ts`の`/api/blinker-etl`・`/api/course-recovery-etl`実装を参照）。

### 2. 【原因未確定・実害は構造的に防止済み】ETL実行中の中断でライブテーブルが壊れる

上記バグ修正後の検証中、`POST /api/blinker-etl`をcurlで直接叩くと**開発サーバーのnode.exeプロセスが実行途中で再起動する**現象が複数回発生した（`netstat`のLISTENING PIDが3900→30892のように変化することをPowerShellの`Get-Process`で確認）。`src/server.ts`に`process.on('uncaughtException'/'unhandledRejection')`のグローバルハンドラを追加しても再発したため、正確なトリガーは特定できていない。

正確な原因を追うのは諦め、**原因を問わず実害だけを防ぐ**方針に切り替えた：`TRUNCATE TABLE X; INSERT INTO X ...` ではなく、

```sql
CREATE TABLE X_NEW LIKE X;
INSERT INTO X_NEW ...;           -- 時間のかかる処理はすべてここで完結させる
RENAME TABLE X TO X_OLD, X_NEW TO X;  -- ほぼ一瞬で完了する原子的操作
DROP TABLE X_OLD;
```
という「別名で新規構築→完成後に原子的にRENAMEで差し替え」方式に統一する（`sql/blinker_index.sql`・`sql/course_recovery_index.sql`は対応済み。`anaba_index.sql`・`honmei_index.sql`・`pacefit_index.sql`は未対応＝同種の症状が出たら同じ対策を入れる）。これにより、実行中に何が起きてライブテーブルが空のまま壊れて放置される事故は構造的に起こらなくなる（直前の正常なデータを提供し続けるだけになる）。

- ETLは状況により2〜6分以上かかることがある。`curl --max-time`のような短いタイムアウトのスクリプトでテストせず、`download.html`のUIボタン（クライアント側タイムアウトなし）を使うか、十分長いタイムアウトを設定する。「進捗が遅い＝失敗」と早合点せず、カバレッジチェックの結果を信じる。

### 3. 【一般的な正しい作法】mysql CLI子プロセスのstdoutは必ず読み捨てる

`src/server.ts`の`runMysqlSql()`は`spawn('mysql', ...)`で子プロセスを起動するが、stdoutを誰も読まないとOSのパイプバッファが満杯になり、診断用SELECT等の出力が多いSQLでmysql CLIがwriteでブロックし続け、`close`イベントが永久に発火しない（＝ETLが無応答のまま完了もエラーも報告されない）事故につながりうる。`proc.stdout.on('data', ()=>{})`で必ず読み捨てること（既に対応済み）。

---

## 作業フロー

- ファイル編集は毎回確認を求めない（常に許可済み）
- 新しい指数・グレード表示を追加するとき、既存の色規約・CSS クラスと整合しているか必ず確認してから実装する
- バックテストスクリプト（`backtest-*.ts` など）は一時ファイルとして作成・実行後に削除する
- テーブル変更時のチェック方法は「UI変更時の必須チェック」セクションに従う

## このファイルの更新ルール

ユーザーが「同じ間違いをしないように」「何度も言っている」「また同じミス」などの言い回しをした場合、
その内容を **即座に CLAUDE.md に追記する**。メモリへの保存だけでは不十分。
